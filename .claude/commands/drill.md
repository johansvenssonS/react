---
description: Set a hands-on exercise in this repo — spec and acceptance criteria, no solution
argument-hint: <topic — e.g. "useState", "lists and keys", "lifting state up">
---

Set Johan an exercise on: **$ARGUMENTS**

Read the relevant project folder's `src/` first (check `CLAUDE.md`'s Project folders list;
ask which folder if it's ambiguous) so the exercise builds on what is already there and is
sized to what he has actually written so far.

Produce:

- **Goal** — one sentence, in terms of what the app does, not what the code contains.
- **Where** — the folder and file(s) to touch, and whether to create or modify.
- **Constraints** — what must be used or avoided so the exercise actually trains the topic.
- **Done when** — 2–4 observable checks Johan can verify in the browser or console.
- **Stretch** — one harder variation.

No solution. No starter code beyond a bare component signature if a file must be created.
After posting it, stop and wait — do not review work that has not been written yet.
